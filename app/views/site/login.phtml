<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: Arial, sans-serif;
            background: #f4f4f4;
        }
        .container {
            display: flex;
            height: 100vh;
        }
        .left {
            flex: 1;
            background: #1a237e;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .logo {
            width: 220px;
            height: 220px;
            background: #fff;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 16px rgba(0,0,0,0.1);
        }
        .logo img {
            max-width: 160px;
            max-height: 160px;
        }
        .right {
            flex: 1;
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .login-form {
            width: 100%;
            max-width: 350px;
            padding: 40px;
            background: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }
        .login-form h2 {
            margin-bottom: 24px;
            color: #1a237e;
            text-align: center;
        }
        .login-form label {
            display: block;
            margin-bottom: 8px;
            color: #333;
        }
        .login-form input {
            width: 100%;
            padding: 10px;
            margin-bottom: 18px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
         button{
            text-align: center;
         }


        .login-form button {
            text-align: center;
            width: 100%;
            padding: 14px;
            background: #1a237e;
            color: #fff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }
        .login-form button:hover {
            background: #3949ab;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="left">
            <div class="logo">
                <!-- Substitua o src abaixo pelo logo da sua empresa -->
                <img src="logo_empresa.png" alt="Logo da Empresa">
            </div>
        </div>
        <div class="right">
            <form class="login-form" method="post" action="<?= DIRPAGE; ?>login/authenticate">
                <h2>Login</h2>
                <label for="username">Usuário</label>
                <input type="text" id="username" name="username" required>
                <label for="password">Senha</label>
                <input type="password" id="password" name="password" required>
                <button type="submit">Entrar</button>
            </form>
        </div>
    </div>
</body>
</html>